apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
 name: chevereto
 annotations:
   ingress.kubernetes.io/ssl-redirect: "false"
spec:
 rules:
 - host: chevereto.172.26.0.4.nip.io
   http:
     paths:
     - path: /
       backend:
         serviceName: chevereto
         servicePort: 80